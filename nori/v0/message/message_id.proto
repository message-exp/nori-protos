syntax = "proto3";

package nori.v0;

import "nori/v0/room/room_id.proto";

/**
 * Message identifier
 */
message MessageId {
  // Room id of the message
  nori.v0.RoomId room_id = 1;
  // 64 bytes snowflake id
  bytes message_id = 2;
}
