syntax = "proto3";

package nori.v0;

/**
 * Request object of logging in with email and password.
 */
message UserEmailPasswordLogin {
  // The email of the user.
  string email = 1;
  // Password to log in.
  string password = 2;
}
