syntax = "proto3";

package nori.common.types.v0;

// 訊息類型
enum MessageType {
  TEXT = 0;
  AUDIO = 1;
  STICKER = 2;
  IMAGE = 3;
  VIDEO = 4;
  FILE = 5;
}

// 使用者狀態
enum UserStatus {
  ONLINE = 0;
  OFFLINE = 1;
  AWAY = 2;
  BUSY = 3;
}

// 事件類型
enum EventType {
  MESSAGE = 0;
  USER_TYPING = 1;
  USER_ONLINE_STATUS = 2;
}
