syntax = "proto3";

package nori.client.role.v0;

import "google/protobuf/timestamp.proto";
import "google/protobuf/color.proto";

// Room member structure
message RoomMember {
  string user_id = 1; // UUID
  string nickname = 2;
  repeated bytes roles = 3; // Member roles, array of strings
}
